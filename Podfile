# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Scholacore' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Scholacore

	pod ‘Firebase/Core’
	pod ‘Firebase/Database’
	pod ‘Firebase/Storage’
	pod ‘Firebase/Auth’
	pod ‘Firebase/Messaging’
    pod 'SDWebImage', '~>3.8'
    pod 'IGListKit'
    pod 'Reusable'
    use_frameworks!
    target 'ScholacoreTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ScholacoreUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
